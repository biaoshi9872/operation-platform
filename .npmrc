# --- 方案一：特定 Scope 走私服 (推荐) ---
# 适用于：只有特定组织（如 @my-company）的包在私服，其他包走公网源
# 语法：@<scope>:registry=<私服地址>
# 示例：让 @my-lib 开头的包走本地私服
@ycb:registry=http://npm.ycb51.cn

# --- 方案二：全局配置 (所有包走私服) ---
# 适用于：私服已经代理了 npm 官方仓库，或者处于内网环境
# registry=http://localhost:4873/

# --- 认证配置 (如果私服需要登录) ---
# 如果私服需要鉴权，通常需要配置 authToken
# //localhost:4873/:_authToken="你的Token"
