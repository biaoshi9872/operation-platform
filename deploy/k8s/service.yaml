apiVersion: v1
kind: Service
metadata:
  name: operation-platform
  namespace: apps
spec:
  selector:
    app: operation-platform
  ports:
    - name: http
      port: 80
      targetPort: http
  type: ClusterIP
