import { setBaseKey } from '@ycb/storage'

export { getLocal, setLocal, getSession, setSession, removeLocal, removeSession, setBaseKey, clearLocal, clearSession } from '@ycb/storage'

setBaseKey('voperate_admin_')
